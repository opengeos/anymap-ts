on:
    push:
        branches:
            - main
    pull_request:
        branches:
            - main

name: Linux build
jobs:
    test-ubuntu:
        runs-on: ubuntu-latest

        strategy:
            fail-fast: false
            matrix:
                python-version: ["3.10", "3.11", "3.12", "3.13"]

        steps:
            - uses: actions/checkout@v6
            - name: Set up Node.js
              uses: actions/setup-node@v6
              with:
                  node-version: "24"
            - name: Install JS dependencies
              run: npm install --legacy-peer-deps

            - name: Run TypeScript tests
              run: npm test

            # - name: Install GDAL system dependencies
            #   run: sudo apt-get update && sudo apt-get install -y gdal-bin libgdal-dev

            - name: Install uv
              uses: astral-sh/setup-uv@v7
              with:
                  version: "0.4.16"
                  enable-cache: false

            - name: Set up Python ${{ matrix.python-version }}
              run: uv python install ${{ matrix.python-version }}

            - name: Install dependencies
              run: |
                  uv venv --python ${{ matrix.python-version }}
                  uv pip install .[all]

            - name: Install test dependencies
              run: |
                  uv pip install pytest pytest-cov nbmake

            - name: Test import
              run: |
                  uv run python -c "from anymap_ts import Map; print('anymap import successful')"

            - name: Running pytest
              run: |
                  uv run pytest . --verbose

            - name: Run notebook integration tests
              run: |
                  uv run pytest --nbmake tests/notebooks/ --nbmake-timeout=120 --verbose
